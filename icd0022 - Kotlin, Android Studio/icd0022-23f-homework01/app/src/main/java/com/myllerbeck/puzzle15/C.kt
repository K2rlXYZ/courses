package com.myllerbeck.puzzle15

object C {
    private const val PREFIX = "com.myllerbeck.puzzle15"
    const val ACTION_TIME = "$PREFIX.time"
    const val ACTION_SERVICE_START = "$PREFIX.service_start"
    const val ACTION_SERVICE_DESTROY = "$PREFIX.service_destroy"
    const val ACTION_SERVICE_PAUSE = "$PREFIX.service_pause"
    const val ACTION_SERVICE_RESUME = "$PREFIX.service_resume"
    const val PAYLOAD_TIME = "$PREFIX.payload.time"

}